name: elevated_ticket_widget
description: A Flutter package which will help you to generate ticket shaped widget with elevation, highly customizable and flexible.
version: 0.0.1
repository: https://github.com/cooltechie-info/elevated-ticket-widget

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
 
flutter:
 
topics:
  - ticket-shape
  - ticket-ui
  - ticket-widget
  - ticket-elevation
  - flutter-ticket
